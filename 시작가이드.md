# Claude Code에서 시작하기 (Windows)

## 1. 준비물 설치 (처음 한 번만)

PowerShell을 열고(시작 메뉴에서 "PowerShell" 검색) 아래를 한 줄씩 실행:

```powershell
irm https://claude.ai/install.ps1 | iex
```

Git이 없다면 https://git-scm.com/downloads/win 에서 설치 (기본 옵션으로 계속 Next).

GitHub 연동 도구(gh)도 설치해 두면 편리:

```powershell
winget install GitHub.cli
```

## 2. 프로젝트 폴더 준비

이 압축파일(my-portfolio.zip)을 풀어서 원하는 위치에 둡니다.
예: `문서\my-portfolio`

## 3. Claude Code 실행

PowerShell에서:

```powershell
cd $HOME\Documents\my-portfolio
claude
```

처음 실행하면 브라우저가 열리며 Claude 계정으로 로그인하라고 안내합니다.
(지금 Cowork에서 쓰는 것과 같은 계정으로 로그인하면 됩니다)

## 4. 첫 요청 (그대로 붙여넣기)

```
CLAUDE.md를 읽고 이 프로젝트를 파악한 뒤, 최우선 작업인 GitHub Pages 배포를 진행해줘.
gh 로그인이 필요하면 단계별로 안내해줘.
```

Claude Code가 CLAUDE.md에 정리된 프로젝트 내용과 작업 순서를 읽고
저장소 생성 → 업로드 → Pages 활성화 → 폰 접속 주소 안내까지 진행합니다.

## 5. 이후 개발

같은 폴더에서 `claude`를 실행하고 원하는 것을 말하면 됩니다. 예:

- "시세 스냅샷을 오늘 종가로 갱신해줘"
- "로드맵 2단계 AI 종목분석 기능을 시작하자"
- "수정한 내용을 GitHub에 올려줘" (폰 앱에 자동 반영됨)
